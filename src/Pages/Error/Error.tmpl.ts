export const template = `
    <main>
        <div class="{{wrapperClass}}">
            <h2 class="{{titleClass}}">
                {{titleText}}
            </h2>
            <span class="{{subtitleClass}}">
                {{subtitleText}}
            </span>
            {{{link}}}
        </div>
    </main>
`;
